
# Server port
server.port=8080

# Application name
spring.application.name=quote-generator-backend

# Enable debug logging (optional)
logging.level.com.inspireflow=DEBUG

# External API configuration
quote.api.url=https://api.quotable.io/random